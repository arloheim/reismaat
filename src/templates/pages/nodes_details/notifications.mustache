<div class="tile is-child box is-flex-grow-0">
  <h3 class="mb-4">meldingen</h3>

  {{#notifications.length}}
    {{#notifications}}
      {{> feed/notification}}
    {{/notifications}}
  {{/notifications.length}}

  {{^notifications.length}}
    <p>Er zijn geen meldingen voor deze halte.</p>
  {{/notifications.length}}
</div>
