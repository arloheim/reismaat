<div class="journey-cell is-node">
  <p>
    <span {{#status}}class="is-{{.}}"{{/status}}>{{> feed/node_short_link}}</span>

    {{#additional}}
      <span class="is-additional-label ml-1">(extra stop)</span>
    {{/additional}}

    {{#cancelled}}
      <span class="is-cancelled-label ml-1">(vervalt)</span>
    {{/cancelled}}
  </p>
</div>